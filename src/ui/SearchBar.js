import React from 'react'
import { StyleSheet } from 'react-native';
import { Icon, Img, Input, SearchBar, Span } from '../Components/Html'

function SearchInput(p) {
  return (
    <SearchBar
      // icon='home'
      src={require('../assets/images/a1.jpg')}
      navigate={'Home'}
      foodAsc={()=>{}}
      Register={()=>{}}
    />
  )
}
export default SearchInput
